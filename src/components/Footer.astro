---
import Logo from './Logo.astro';
const currentYear = new Date().getFullYear();

// Define your navigation links here to keep the HTML clean
const links = [
  { name: 'Features', href: '#features' },
  { name: 'How It Works', href: '#how-it-works' },
  { name: 'Testimonials', href: '#testimonials' },
  { name: 'Pricing', href: '#pricing' },
];

const legalLinks = [
  { name: 'Terms of Service', href: '/terms' },
  { name: 'Privacy Policy', href: '/privacy-policy' },
  { name: 'Refund Policy', href: '/refund-policy' },
];
---

<footer class="bg-secondary/50 border-border border-t px-3 py-16">
  <div class="mx-auto max-w-5xl px-4">
    <div class="flex flex-col justify-between gap-12 md:flex-row">
      <div class="max-w-sm space-y-4">
        <div class="flex items-center gap-2">
          <Logo sizeClass="h-10" />
        </div>
        <p class="leading-relaxed text-slate-500">
          The most elegant way to build your affiliate marketing program for
          your SaaS business.
        </p>
      </div>

      <div>
        <h4 class="mb-4 font-bold text-slate-900">Explore</h4>
        <ul class="space-y-2">
          {
            links.map((link) => (
              <li>
                <a
                  href={link.href}
                  class="hover:text-primary text-slate-500 transition-colors"
                >
                  {link.name}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>

    <div
      class="border-border mt-12 flex flex-col items-center justify-between gap-4 border-t pt-8 md:flex-row"
    >
      <p class="text-sm text-slate-500">
        &copy; {currentYear} RefearnApp. All rights reserved.
      </p>

      <div class="flex flex-wrap justify-center gap-x-6 gap-y-2 text-sm">
        {
          legalLinks.map((link) => (
            <a
              href={link.href}
              class="hover:text-primary text-slate-500 transition-colors"
            >
              {link.name}
            </a>
          ))
        }
      </div>
    </div>
  </div>
</footer>
